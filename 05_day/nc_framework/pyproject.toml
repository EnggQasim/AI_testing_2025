[project]
name = "nc-framework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sir Qasim", email = "m.qasim077@gmail.com" }
]
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.24.4",
    "scikit-learn>=1.3.2",
    "torch>=2.5.1",
]

[project.scripts]
nc-framework = "nc_framework:main"
demo = "nc_framework:demo_confusion_metrics"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
